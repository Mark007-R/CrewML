# Model Card — CrewML on `credit-g`

*Generated by the CrewML crew's Reporter node. Every number below is a
**cross-validated estimate on the training split**, not a held-out score
(`cv_score_is_holdout: false`). The sealed held-out evaluation is Phase 3.*

## Model details

- **Dataset:** `credit-g` — classification (binary), primary metric **ROC AUC**.
- **Final model:** Single model: **random_forest** (ensemble did not improve on it).
- **Headline CV score:** **0.7994 ROC AUC** (mean over 5-fold StratifiedKFold).
- **Built by:** the full seven-node crew (Profiler → Planner → Feature Engineer →
  Trainer → Critic → Ensembler → Reporter) over 1 Critic pass(es).

## Training data

- **Rows (train split):** 800 · **features profiled:** 20.
- **Data flags:** class_imbalance, mixed_dtypes.
- **Columns dropped by the plan:** none (leakage / integrity grounds).
- **Feature engineering:** `default` — added ['row_nan_count'] column(s).
- The **held-out split was never loaded** during modeling — it is reserved for final scoring.

## Evaluation

- **Protocol:** StratifiedKFold(5), scorer `roc_auc`, seeded for reproducibility.
- **Class imbalance handling:** enabled (class_weight=balanced).

### Per-candidate CV (ROC AUC)

| model | CV mean | CV std |
|-------|:-------:|:------:|
| hist_gradient_boosting | 0.7703 | ±0.0529 |
| random_forest | 0.7994 | ±0.0623 |
| logistic_regression | 0.7907 | ±0.0700 |

### Critic loop

| pass | decision | findings | CV score |
|:----:|----------|----------|:--------:|
| 1 | finalize | none | 0.7994 |

## LLM assistance

- Advisory narratives (never decision-making): **none live (3 requested, all unavailable/mock)**.
- The crew's every *decision* is deterministic; the LLM only ever adds an optional
  written second opinion, so a degraded provider never changes what the crew does.

## Limitations & honest caveats

- All scores are CROSS-VALIDATED estimates on the train split (cv_score_is_holdout: false); the sealed held-out score is Phase 3.
- No advisory LLM narrative ran live this run; every decision stands on the deterministic core.

- The executor is a **process-isolation** sandbox, not yet a security sandbox
  (hardening is Phase 4 / Day 19).
- "Overfit" signals from the Critic are **cross-validation fold-instability**, not a
  train-vs-held-out gap — the crew cannot see the held-out split by construction.
