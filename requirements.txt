# --- Core data / ML ---
# Bounds added Day 27 for container/host parity: the Day-1 holdout seals are
# SHA-256 over `df.to_parquet(index=False)` bytes, so the verifying
# environment must (de)serialize EXACTLY like the sealing one — pandas is
# pinned exact because 2.3/3.0 re-serialize the same untouched file to
# different bytes and flip holdout_untouched to False (verified empirically:
# 2.1.4 reproduces the sealed hashes bit-for-bit, 2.3.3 and 3.0.5 do not).
# sklearn is bounded for image parity too (1.9 pulls narwhals into
# sklearn.utils.validation). File-byte seals that free these pins are a
# Phase 6 candidate.
scikit-learn>=1.4,<1.9
pandas==2.1.4
numpy>=1.26,<2.0
pyarrow>=15.0,<19
scipy>=1.11,<1.14

# --- Dataset sourcing ---
openml>=0.14

# --- Multi-agent orchestration (added in Phase 2) ---
langgraph>=0.2
langchain-core>=0.3

# --- LLM providers ---
groq>=0.11
# Optional second provider for the Day-16 provider study. Imported lazily in
# crewml/llm.py, so the crew runs fine without a key — but the Claude arm cannot
# run at all from a clean install unless the SDK is installed.
anthropic>=0.40
python-dotenv>=1.0

# --- Classical AutoML baseline (Day 4) ---
# FLAML + the gradient-boosting backends it selects across the suite. catboost is
# NOT in flaml[automl] but FLAML picked it for kin8nm, so pin it explicitly for
# reproducibility (else that learner is silently skipped on a fresh install).
flaml[automl]>=2.1
lightgbm>=4.0
xgboost>=2.0
catboost>=1.2

# --- Charts (Phase 3 comparison studies) ---
matplotlib>=3.8

# --- Serving / dashboard (Phase 5) ---
fastapi>=0.110
uvicorn>=0.29
streamlit>=1.33

# --- Tooling ---
pytest>=8.0

# --- Production API (Phase 5, Day 24) ---
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
# Day 26: multipart CSV upload (FastAPI needs it for UploadFile/Form routes)
python-multipart>=0.0.9
# Day 27: optional shared node-cache backend (docker-compose wires
# CREWML_REDIS_URL=redis://redis:6379/0; unset => file cache, no server needed)
redis>=5.0
